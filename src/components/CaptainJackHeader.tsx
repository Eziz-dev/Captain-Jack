type CaptainJackHeaderProps = {
  className?: string;
};

const CaptainJackHeader = ({ className }: CaptainJackHeaderProps) => {
  return <h1 className={className}>Captain Jack</h1>;
};

export default CaptainJackHeader;
